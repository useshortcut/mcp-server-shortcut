{
  "name": "shortcut-mcp-worker",
  "version": "0.1.0",
  "description": "Shortcut MCP Server running on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5",
    "@modelcontextprotocol/sdk": "^1.6.1",
    "@shortcut/client": "^1.1.0",
    "@shortcut/mcp-tools": "file:../shortcut-mcp-tools",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240515.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}